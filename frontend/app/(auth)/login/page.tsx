import { LoginForm } from "@/components/auth/login-form";

export default function LoginPage() {
  return (
    <main className="spmi-login-page">
      <section className="spmi-login-card">
          <LoginForm />
      </section>
    </main>
  );
}
